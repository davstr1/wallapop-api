# Server
PORT=4000

# Proxy (required — Wallapop blocks direct server requests)
# Format: http://user:pass@host:port
PROXY_URL=

# Optional: override base URL (default: https://api.wallapop.com/api/v3)
# API_BASE_URL=https://api.wallapop.com/api/v3
